To run:

```npm start```

## Assumptions and considerations:
### Global:
- I'm hardcoding all strings initially, considering i18n was never mentioned in the task spec
### Search:
- Assuming there's no suggest/autocomplete for the search bar, since not mentioned in the spec
- Took a shortcut with inlining the svg as background image, since centralized styling was not mentioned as a requirement. A proper way would be to add svg components to enable interaction and styling.